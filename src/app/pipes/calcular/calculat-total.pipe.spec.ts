import { CalculatTotalPipe } from './calculat-total.pipe';

describe('CalculatTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
